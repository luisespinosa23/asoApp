import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  HomeContainer: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "space-around",
  },
});
