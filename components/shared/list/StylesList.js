import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  EmptyTextContainer: {
    flexGrow: 1,
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  },
  EmptyText: {
    fontWeight: "bold",
    fontSize: 20,
  },

  ListContainer: {
    height: "100%",
    paddingLeft: 10,
    paddingRight: 10,
  },
});
