import { StyleSheet } from "react-native";
import { colors } from "./Colors";

export const stylesInputError = StyleSheet.create({
  ErrorMessage: {
    color: colors.errorTextColor,
    paddingTop: 5,
    paddingBottom: 5,
    fontSize: 16,
  },
});
